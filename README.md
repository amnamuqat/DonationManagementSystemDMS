Donation Management System (DMS)
Course:Object-Oriented Programming (OOP)
Supervised By: Suheir Harb
Student Name:Amna H.Z Makat(194901137)
             Mahmoud Jmail Nasralah(194901163)
             Aya  Rami Ghanem(2049011004)
             
Project Description:The Donation Management System (DMS) is a Java Swing desktop application developed to streamline charity operations through centralized
                    management of donors, donations, financial records, and aid distribution. With secure role
                    
 Class:JFrame (super) , MainFrame(sub),AdminLogin, AddDonation, AdminSection, AddAssistant, ViewAssistant, ViewDonation, AssistantLogin,AssistantSection   AddAssistant , DeleteDonation,Help,Assistant,Donation,Edit Donation, MultiSelectCombo
 
 Object-Oriented Programming Principles:Encapsulation,Inheritance,Polymorphism
 
Donation Management System * Advanced Multi-Selection Filter: Embeds a custom composite UI component ( MultiSelectCombo) enabling asynchronous selection of multiple t a r ge t beneficiary groups Main Login Screen: This screen allows the user to select the type of login. The user can choose either Admin Login to access the administrator functions or Donor Login to access the donor account. This is the first screen displayed when the application starts

Login Screen and track active donation records via interactive layout grids. *Advanced Multi-Selection Filter: Embeds a custom composite UI component Admin Login Screen This screen allows the administrator to log into the system by entering the correct username and password. If the entered credentials are valid, the administrator is User name :admin Password:admin123

Donor Login Screen This screen allows registered donors to log into the system. A donor can only log in using an account that has already been created by the administrator through the Add Donor function. For example, if the  Userdonor: amna Password:123456

Admin Section • This screen is the main control panel for the administrator after a successful login. It provides the administrator with the main functions required to manage the system. The available options are: • Add Donor: Allows the administrator to register a new donor by entering the donor's information. The newly created donor account can later be used to log into the system. • View Donors: Displays a list of all registered donors. From this section, the administrator can view donor information and manage donor records. • • Logout: Allows the administrator to securely exit the system and return to the main login screen 

Add Donor Screen This screen allows the administrator to register a new donor in the system. The administrator enters the donor's information, including the Donor Name, Password, Email, and Contact Number. After filling in all the required fields, clicking the Add Donor button saves the donor's information in the system. The donor can later use the Donor Name and Password that were created by the administrator to log into the Donation Management System. The Email and Contact Number are stored as contact information for communication with the donor when needed. The Back button returns the administrator to the previous screen without adding a new donor. 

Admin Section • This screen is the main control panel for the administrator after a successful login. It provides the administrator with the main functions required to manage the system. The available options are: • Add Donor: Allows the administrator to register a new donor by entering the donor's information. The newly created donor account can later be used to log into the system. • View Donors: Displays a list of all registered donors. From this section, the administrator can view donor information and manage donor records. • • Logout: Allows the administrator to securely exit the system and return to the main login screen 

dd Donor Screen This screen allows the administrator to register a new donor in the system. The administrator enters the donor's information, including the Donor Name, Password, Email, and Contact Number. After filling in all the required fields, clicking the Add Donor button saves the donor's information in the system. The donor can later use the Donor Name and Password that were created by the administrator to log into the Donation Management System. The Email and Contact Number are stored as contact information for communication with the donor when needed. The Back button returns the administrator to the previous screen without adding a new donor. (Quantity * Unit Price) handled seamlessly by the core application thread. *Dual-Gate Authentication: Implements explicit role boundaries via distinct secure access portals for Administrators (AdminLogin) and Assistants (AssistantLogin). 

View Donors Screen composite UI component (MultiSelectCombo) enabling selection of multiple target beneficiary groups concurrently. This screen is used only to display the list of donors registered in the system. It shows each donor's ID, Donor Name, Password, Email, and Contact Number in a table for easy viewing.  The Back button returns the administrator to the previous screen. 

Donor Section (MultiSelectCombo) enabling asynchronous selection of multiple target beneficiary groups concurrently. This screen contains the main options available for the donor. It includes the following buttons: • Add Donation • View Donations • Edit Donation • Delete Donation • Logout

Add Donation Screen (Quantity * Unit Price) handled seamlessly by the core application thread. * This screen allows the donor to add a new donation to the system. The donor enters the Donation Name, selects the Donation Type, enters the Amount and Quantity, and the Total value is calculated automatically. The donor also selects the Target Group for the donation, writes a Description to provide additional details, and chooses the Donation Status (such as Available). After completing all the required information, clicking the Add Donation button saves the donation, while the Back button returns to the previous screen. 

View Donations Screen and track active donation records via interactive layout grids. This screen is used only to display the donations stored in the system. It presents the donation information in a table, including the Donation Name, Donation Type, Amount, Quantity, Total, Target Group, Description, and Status. This screen is for viewing purposes only and does not allow the donor to add, edit, or delete donations. The Back button returns the user to the previous screen. 

Edit Donation Screen * Dynamic CRUD Capabilities: Provides assistants with real-time abilities to append, view, and This screen allows the donor to update an existing donation. The donor first enters the Donation ID and clicks the Load button to retrieve the donation information from the system. After the data is loaded, the donor can modify the donation details, such as the Donation Name, Donation Type, Amount, Quantity, Target Group, Description, and Status. Once the required changes are completed, clicking the Update Donation button saves the updated information in the system. The Back button returns the donor to the previous screen. 

View Donors Screen composite UI component (MultiSelectCombo) enabling selection of multiple target beneficiary groups concurrently. This screen is used only to display the list of donors registered in the system. It shows each donor's ID, Donor Name, Password, Email, and Contact Number in a table for easy viewing.  The Back button returns the administrator to the previous screen. 


Donor Section (MultiSelectCombo) enabling asynchronous selection of multiple target beneficiary groups concurrently. This screen contains the main options available for the donor. It includes the following buttons: • Add Donation • View Donations • Edit Donation • Delete Donation • Logout

Add Donation Screen (Quantity * Unit Price) handled seamlessly by the core application thread. * This screen allows the donor to add a new donation to the system. The donor enters the Donation Name, selects the Donation Type, enters the Amount and Quantity, and the Total value is calculated automatically. The donor also selects the Target Group for the donation, writes a Description to provide additional details, and chooses the Donation Status (such as Available). After completing all the required information, clicking the Add Donation button saves the donation, while the Back button returns to the previous screen. 

View Donations Screen and track active donation records via interactive layout grids. This screen is used only to display the donations stored in the system. It presents the donation information in a table, including the Donation Name, Donation Type, Amount, Quantity, Total, Target Group, Description, and Status. This screen is for viewing purposes only and does not allow the donor to add, edit, or delete donations. The Back button returns the user to the previous screen. 

Edit Donation Screen * Dynamic CRUD Capabilities: Provides assistants with real-time abilities to append, view, and This screen allows the donor to update an existing donation. The donor first enters the Donation ID and clicks the Load button to retrieve the donation information from the system. After the data is loaded, the donor can modify the donation details, such as the Donation Name, Donation Type, Amount, Quantity, Target Group, Description, and Status. Once the required changes are completed, clicking the Update Donation button saves the updated information in the system. The Back button returns the donor to the previous screen. 

Delete Donation Screen This screen allows the donor to delete an existing donation from the system. The donor enters the Donation ID of the donation to be removed and then clicks the Delete button. If the entered ID exists, the corresponding donation is deleted from the system. The Back button returns the donor to the previous screen without deleting any data. 

Conclusion * Advanced Multi-Selection Filter: Embeds a custom composite UI component (MultiSelectCombo) enabling asynchronous selection of multiple target beneficiary groups concurrently. In this project, we developed a simple Donation Management System using Java. The system provides two types of users: Admin and Donor. The administrator can add and view donors, while donors can add, view, edit, and delete donations. The project helped us apply objectoriented programming concepts, graphical user interface design, and data management in a practical application.  Overall, the system provides a simple and organized way to manage donors and donations through an easy-to-use interface. 

"I confirm that AI tools were only used as an editing and debugging aid to resolve technical errors and refine the codebase of this project."
